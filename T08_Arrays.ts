const superHeros: string[] = [];
const superPower: number[] = [];
superHeros.push("spiderman");
superPower.push(1);


// another way of doing the above things
let arr:Array<number> = []

// in the same way we can make the object of Array


// 2D
const arr1 : Array<Array<number>> = [[]];

// matrix
let arr2:number[][] = [
    [255, 255, 255]
];